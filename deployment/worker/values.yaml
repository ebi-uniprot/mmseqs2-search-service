# Default values for worker.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
# update below config with your RabbitMQ details
rabbitmq:
  host: mmseqs-rabbitmq
  port: 5672
  queueName: task_queue
  userName: user
  password: 0CTopmP8ppyZp9zi

metadb:
  host: my-service
  port: "8080"