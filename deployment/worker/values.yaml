# Default values for worker.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
# update below config with your RabbitMQ details
rabbitmq:
  host: mmseqs2-queue-rabbitmq
  port: 5672
  queueName: task_queue
  userName: user
  password: mypassword123

metadb:
  host: mmseqs2-metadb
  port: "8080"
